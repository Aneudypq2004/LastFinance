import { IconCoin } from "@tabler/icons-react"

export default function Logo() {
    return (
        <h1 className="px-8 py-2 flex gap-1"> {<IconCoin className="text-naranja" size={30} />} LAST<sub>Finance</sub></h1>
    )
}
